name: CI

on:
  workflow_dispatch:
  push:
  pull_request:
    branches: main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: step
      run: |
        set -ux
        lscpu
        df -h
        mount
        ls -la /mnt/**
        cat /mnt/DATALOSS_WARNING_README.txt
