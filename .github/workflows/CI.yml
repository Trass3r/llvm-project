name: CI

on:
  workflow_dispatch:
  push:
  pull_request:
    branches: main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: step
      run: |
        set -eux
        lscpu
        df -h
